# 三角洲助手视觉系统

## 概念主张

三角洲助手的结果图不是游戏 HUD，也不是一组通用信息卡，而是一份刚从黑色工作台上完成核验的货运文件。摸金结果表现为“战利品查验单”，每日密码表现为“地图密码调度单”。两者共享墨黑工作台、安保橙货签、骨白清单纸、直角框线、装订孔和人工盖章感，形成可识别的“货运查验”世界。

信息叙事固定为三步：先在左侧货签确认对象、日期或最高结论；再在右侧清单逐行核对核心数据；最后在页脚确认模拟声明、来源和更新时间。图片增强阅读，但不得承载文本兜底中不存在的关键事实。

## 颜色与令牌

| 令牌 | 色值 | 用途 |
| --- | --- | --- |
| `--ink` | `#10110f` | 工作台背景、主文字、粗分隔线与装订孔 |
| `--ink-soft` | `#242620` | 类别标记等次级深色表面 |
| `--paper` | `#f2eddf` | 主清单纸张，避免纯白的屏幕感 |
| `--paper-muted` | `#d9d1bd` | 备用纸张层级，仅在需要区分纸面时使用 |
| `--orange` | `#ee6a24` | 左侧安保货签、选择高亮与唯一的大面积强调色 |
| `--orange-deep` | `#9f3511` | 印章、密码和纸面上的橙色细节 |
| `--line` | `rgba(16, 17, 15, 0.25–0.26)` | 纸面内框、表头和行分隔线 |
| `--white` | `#f7f5ec` | 白色品质标识 |
| `--green` | `#4d9f69` | 绿色品质标识 |
| `--blue` | `#3f82c4` | 蓝色品质标识 |
| `--purple` | `#8361bf` | 紫色品质标识 |
| `--gold` | `#c88a1e` | 金色品质标识 |
| `--red` | `#c23d35` | 红色品质标识 |

橙色约占画面三成，集中在左侧货签，不扩散为大量按钮或彩色卡片。纸面保持骨白，墨黑承担结构。品质色只用于窄色条等辅助识别，必须同时显示品质文字。

## 字体

- 界面正文与标题使用 `system-ui, -apple-system, Segoe UI, Microsoft YaHei UI, PingFang SC, Noto Sans CJK SC, sans-serif`。模板不依赖某一种专有字体的固定字宽，布局必须容忍操作系统字体替换。
- 大标题、结论和日期通过 800–900 字重、短行宽和紧凑行高形成货签编号与仓储标牌感，不依赖窄体字体特性。
- 四位密码使用 `ui-monospace, SFMono-Regular, Consolas, Liberation Mono, monospace`，保留前导零并启用等宽数字。
- 大标题为 58–62px，清单标题为 42px，核心行名称为 22–24px，密码为 37px，正文和元数据为 13–18px。
- 标题允许紧字距，正文不使用过度字距。英文印章和小型标签可以使用大写与适度字距，但不能替代中文语义。

## 1120px 输出几何

所有输出固定宽度为 1120px，画布背景为墨黑，禁止页面滚动。外边距统一为 24px，两栏间距为 18px；内容区宽度为 1072px，左栏固定 326px，右栏为 728px。左栏约占有效宽度三成，负责识别与结论；右栏约占七成，负责逐项核对。

- 战利品查验单尺寸为 `1120 × 720px`，两栏可用高度为 672px。右侧清单内边距为上 32px、左右 36px、下 24px，最多稳定展示 4 条物品。
- 地图密码调度单尺寸为 `1120 × 850px`，两栏可用高度为 802px。右侧沿用相同内边距，稳定展示 6 条地图记录。
- 纸面内框距离边缘 12px；主标题区最小高度 104px；清单底部使用 2px 墨黑线收束来源或声明。
- 左侧从距边缘 12px 的装订孔列开始，正文左内边距为 46px。装订孔纵向均匀分布，作为物理文件而非数字面板的关键证据。
- 阴影只用于把货签和纸张从工作台上抬起，统一采用约 `0 18px 34px` 的柔和深色阴影；不要新增悬浮卡片层级。

## 组件解剖

### 左侧货签

货签由品牌名、对象标题、日期或结论章、关键元数据以及装订孔组成。标题是第一视觉入口；结论章使用双线边框和约 `-1.2deg` 至 `-1.4deg` 的轻微旋转，模拟人工盖章。右下角的大型半透明斜框只作材质纹理，不承载信息。

战利品货签显示容器名称、最高品质、容器档位、物品数量和货单编号。密码货签显示标题、日期、数据状态、覆盖地图数和数据日期。两者结构相同，但字段不得为了视觉对齐而伪造。

### 右侧清单纸

清单纸由标题区、列标题、数据行和页脚组成。标题区右上角保留旋转的英文核验章，提供文件类型识别；中文标题和说明承担实际语义。列标题是辅助定位，不应比数据行更醒目。

战利品行包含序号、类别短标、物品名称、完整类别、品质色条和品质文字。密码行包含序号、地图名、位置和四位密码。所有行使用直角、细分隔线和稳定列宽，不使用圆角容器、渐变、玻璃效果或独立卡片阴影。

### 页脚

战利品页脚必须展示模拟声明和目录版本来源；密码页脚必须展示数据来源与更新时间。页脚是可信度边界，不能因空间不足而隐藏，也不能把旧数据描述为今日数据。

## 数据密度规则

- 一张图只回答一个问题：本次摸到了什么，或今天各地图的密码是什么。
- 战利品最多显示 4 行，单行最小高度 88px；密码固定按地图顺序最多显示 6 行，单行最小高度 88px。
- 左栏只保留 2–3 组决策级元数据；详细类别、位置、来源等放入右侧清单。
- 核心值必须在首屏内完整可见，不允许滚动、分页提示或折叠内容。
- 不通过缩小到难以阅读的字号换取更多行。数据超过容量时，应在业务层拆分输出、截断到明确上限并说明，或改用文本兜底。
- 数字使用等宽或表格数字，密码始终按字符串渲染，四位不足时保留前导零。

## 空数据与长文本

模板不应把空数组渲染成一张看似成功的空货单。无战利品、密码未获取、数据日期不符或上游校验失败时，由业务层返回明确的终态文本；若未来需要图片空态，应保留同一货签与纸面结构，在右侧以单一核验结论替代数据行，并说明原因和可执行的下一步。

容器名和元数据允许 `overflow-wrap: anywhere`，但应优先在数据层限制为短名称。战利品名称最多占主要名称列，不得挤压品质文字；超长名称应换行并维持 88px 行高可读，无法容纳时由业务层缩短别名。地图位置最多两行，超出部分使用两行截断；完整位置仍须出现在文本兜底中。标题超过两行、来源过长或记录数超过模板容量时，不得依赖 CSS 缩放整个画面。

## 无障碍约束

- 关键状态不能只依靠颜色。品质色条必须与“白、绿、蓝、紫、金、红”等文字并列；缓存状态、日期和来源必须直接写出。
- 墨黑与骨白、墨黑与安保橙之间保持高对比。浅灰仅用于次要说明，不能用于密码、物品名或错误状态。
- HTML 使用清晰的标题、列表、定义列表和时间元素；纯装饰装订孔使用 `aria-hidden="true"`。
- 列标题即使对视觉阅读有帮助，也不能成为理解数据的唯一手段；每一行的 DOM 顺序应与视觉顺序一致。
- 图片外的文本回复必须包含容器、物品、品质、地图、密码、日期、来源状态等全部关键内容。
- 不使用闪烁、动画或依赖鼠标悬停的信息。输出是静态图片，所有内容在首次渲染时可见。

## 未来模板的延续规则

新增模板必须先回答“它在货运查验现场中是什么文件”。可以扩展为封存记录、撤离清单、物资盘点表或风险告知单，但不能退回通用仪表盘、霓虹游戏 HUD、圆角卡片墙或品牌海报。

未来模板应继续采用 1120px 固定画布、约三七分栏、橙色实体货签与骨白主文件的关系，并复用直角框线、装订孔、核验章、编号、来源页脚和轻微人工偏转。变化应来自文件职责：字段、行结构、盖章内容和纸面比例可以调整；世界观锚点、信息叙事和可信度边界不可丢失。

引入新状态色、图标或纹理前，应先证明现有墨黑、安保橙、骨白与文字标签无法表达。不得使用来源不明的游戏素材或制造官方背书。任何看似“战利品爆率”“实时密码”或“已验证”的表达，都必须与插件实际校验能力一致。
