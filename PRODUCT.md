# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

Go 1.26.6、RayleaBot Go SDK v0.4.0，以及由宿主 Render Service 渲染的静态 HTML/CSS 模板。

## Users

RayleaBot 群聊和私聊中的《三角洲行动》玩家。他们希望通过短指令进行一次娱乐化摸金抽取，或快速查看当天的地图密码。

## Product Purpose

插件提供两项能力：根据容器专属物品池生成一次明确标注为模拟结果的摸金卡片；获取、校验并展示北京时间当天的地图密码。成功标准是指令短、结果易读、来源日期明确，并在图片渲染或上游接口不可用时提供安全的文本结果。

## Positioning

容器模拟不是无差别抽奖。每种容器拥有独立别名、品质档位、物品类别和候选物品，结果保留《三角洲行动》容器之间的差异，同时明确说明概率属于插件娱乐配置。

## Operating Context

- 用户发送 `/摸航空箱`、`/摸 航空箱`、`/可摸容器`、`/三角洲密码` 或 `/每日密码`。
- RayleaBot 解析命令并将群聊或私聊事件投递给插件。
- 插件通过宿主 `render.image` 生成结果图，通过 `message.send` 发送图片或文本。
- 每日密码通过受限 HTTPS 请求获取，按北京时间校验日期，并把当天有效结果缓存到插件私有 KV。

## Capabilities and Constraints

- 插件位于独立目录，不修改 RayleaBot 主仓库。
- manifest 使用 v3，协议使用 RayleaBot plugin protocol v2。
- 首版支持常规环境容器；活动容器、玩家盒子和账号数据不在范围内。
- 容器概率是公开资料指导下的娱乐模拟权重，不宣称为官方掉落概率。
- 密码只有在日期、地图名和四位数字均通过校验时才可展示；旧日期缓存不能冒充今日数据。
- 远程失败、渲染失败和不支持的容器都必须产生可理解的终态回复。
- 不复制第三方 AGPL 插件代码、模板或来源不明的游戏素材。

## Brand Commitments

名称使用“三角洲助手”。用户可见文案直接、克制，保留摸金语境，但不制造官方背书、必中暗示或真实爆率声明。

## Evidence on Hand

- 用户确认的插件方案和功能边界。
- RayleaBot manifest v3、protocol v2、Go SDK 与独立插件构建流程。
- 联网核验的容器类别资料、相对爆率分级和每日密码 JSON 接口。
- 没有可验证的官方精确爆率，也没有获得授权的官方品牌素材。

## Product Principles

- 模拟与事实分开标注。
- 当日数据宁缺毋滥。
- 图片增强阅读，文本保证可用。
- 数据表可版本化，业务逻辑不依赖单一上游返回形状。
- 默认不收集用户隐私或游戏账号凭据。

## Accessibility & Inclusion

图片不能只依靠颜色表达品质；每件物品同时显示品质文字。文本兜底包含图片中的全部关键信息。
